<header>
  <h1><%= @category.title %></h1>
</header>

<summary>
  <h3><%= @category.description %></h3>
</summary>

<p><%= link_to 'Edit', edit_category_path(@category) %>
<p><%= link_to 'Delete', category_path(@category), method: 'delete', data: {confirm: "Really want to delete this category?"}  %></p>
